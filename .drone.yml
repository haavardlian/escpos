pipeline:
    test:
        image: node:7.5.0
        commands:
            - npm install -q
            - npm run lint -q
        when:
            branch: master
            event: [push, pull_request, tag, deployment]
    test:
        image: node:7.5.0
        commands:
            - npm install -q
            - npm run lint -q
        when:
            event: tag
    publish:
        image: plugins/npm
        folder: dist
        when:
            event: tag